interface:
  display_name: "Codex Long Task Bootstrap"
  short_description: "Recover long projects with durable state."
  default_prompt: "Use $codex-long-task-bootstrap to recover this project, verify the Git checkpoint, and continue the next bounded issue."
